# Neovim 离线闯关全能实战题库 (The Offline Neovim Mastery Quest)

> 💡 **离线无网使用声明**：  
> 本题库与配套代码仓库完全自包含！离开网络前只需 `git pull` 一次，后续在任何断网环境（飞机、高铁、离线工作站）均可随时打开本手册，对照题目在代码中逐题闯关演练。

---

## 🎯 闯关总则与演练准备

进入项目根目录：
```bash
cd /home/tetsuya/development/night-batch-lab
```

所有题目均依托以下两个演练战场：
1. **多模块项目工程**：[`examples/playground/`](file:///home/tetsuya/development/night-batch-lab/examples/playground/)（包含 `main.py`, `service.py`, `engine.py`, `models.py`, `config.py`）
2. **单文件综合演练场**：[`examples/ui_playground.py`](file:///home/tetsuya/development/night-batch-lab/examples/ui_playground.py)

---

## 目录（39 道实战闯关题）
- [第一关：跨文件工程与 LSP 联动篇 (第 01 ~ 05 题)](#第一关跨文件工程与-lsp-联动篇)
- [第二关：精准光标与极速移动篇 (第 06 ~ 09 题)](#第二关精准光标与极速移动篇)
- [第三关：高能文本对象与并发编辑篇 (第 10 ~ 15 题)](#第三关高能文本对象与并发编辑篇)
- [第四关：折叠与视觉透视篇 (第 16 ~ 19 题)](#第四关折叠与视觉透视篇)
- [第五关：色彩魔法与界面调优篇 (第 20 ~ 22 题)](#第五关色彩魔法与界面调优篇)
- [第六关：剪贴板时空穿梭篇 (第 23 ~ 25 题)](#第六关剪贴板时空穿梭篇)
- [第七关：Git 审查与版本时间机篇 (第 26 ~ 29 题)](#第七关git-审查与版本时间机篇)
- [第八关：文件系统与工程级重构篇 (第 30 ~ 32 题)](#第八关文件系统与工程级重构篇)
- [第九关：交互式状态栏与沉浸控制台篇 (第 33 ~ 35 题)](#第九关交互式状态栏与沉浸控制台篇)
- [第十关：全工程内容搜索与批量替换篇 (第 36 ~ 39 题)](#第十关全工程内容搜索与批量替换篇)

---

## 第一关：跨文件工程与 LSP 联动篇

### 第 01 题：跨文件定义瞬移 (Go to Definition)
- **考查点**：LSP 原生跳转与历史回溯 (`gd` / `Ctrl+o`)
- **目标文件**：`examples/playground/main.py`
- **任务目标**：从调度入口文件一路跳转到最深层的底层模型与引擎代码。
- **操作步骤**：
  1. 打开入口文件：`nvim examples/playground/main.py`
  2. 移动光标到第 45 行的 `BatchOrchestratorService` 单词上，按 **`gd`**。
  3. 此时进入了 `service.py`。移动光标到第 34 行的 `CalculationEngine` 上，按 **`gd`**。
  4. 此时进入了 `engine.py`。
  5. 按 **`Ctrl+o`**（Jump backward）：光标瞬间返回 `service.py`；再按一次 **`Ctrl+o`**，瞬移返回 `main.py`！
- **通关验证**：成功在 3 个文件之间穿梭自如，并且能用 `Ctrl+o` 顺畅返回。

---

### 第 02 题：跨模块悬浮侦探 (Hover Documentation)
- **考查点**：LSP 文档悬浮查询 (`K`)
- **目标文件**：`examples/playground/service.py`
- **任务目标**：不打开源文件，就地查阅另一个文件里类的注释与签名。
- **操作步骤**：
  1. 在 `service.py` 第 41 行，找到 `PipelineExecutionPlan` 实体。
  2. 把光标停在该类名上，按下大写 **`K`**。
  3. 观察弹出的半透明悬浮卡片。
- **通关验证**：屏幕中央弹出悬浮窗，完整展示了 `models.py` 里为 `PipelineExecutionPlan` 编写的中文 Docstring 注释。再按一次 `Esc` 或移动光标，浮窗自动关闭。

---

### 第 03 题：跨工程符号重命名 (LSP Rename)
- **考查点**：跨文件安全重命名 (`<leader>cr`)
- **目标文件**：`examples/playground/service.py`
- **任务目标**：修改某个方法名，让调用它的所有文件同时自动更新。
- **操作步骤**：
  1. 在 `service.py` 第 38 行，将光标放在方法名 `create_mock_pipeline` 上。
  2. 按快捷键 **`<leader>cr`**（先按空格，再按 `c`，再按 `r`）。
  3. 此时屏幕弹出重命名输入框，输入新名字 `build_test_pipeline`，回车确认。
  4. 打开 `main.py`（输入 `:e examples/playground/main.py`）。
- **通关验证**：查看 `main.py` 第 48 行，原来调用的 `create_mock_pipeline` 已经被自动且精准地更新为 `build_test_pipeline`！

---

### 第 04 题：多文件标签极速穿梭 (Bufferline Navigation)
- **考查点**：顶部多标签切换与关闭 (`<S-h>` / `<S-l>` / `<leader>bd`)
- **任务目标**：同时打开 4 个代码文件并在它们之间极速横跳。
- **操作步骤**：
  1. 在 Neovim 中输入命令：`:e examples/playground/config.py`，再输入 `:e examples/playground/models.py`。
  2. 观察编辑器最上方的标签栏（Bufferline），此时同时显示了多个已打开文件的名字。
  3. 按 **`<S-h>`**（Shift + h）或 **`[b`**：向左切换一个文件标签。
  4. 按 **`<S-l>`**（Shift + l）或 **`]b`**：向右切换一个文件标签。
  5. 切换到 `config.py`，按下 **`<leader>bd`**。
- **通关验证**：`config.py` 标签被安全关闭，当前窗口自动显示相邻的文件标签，没有关闭整个 Neovim 窗口。

---

### 第 05 题：代码大纲鸟瞰导航 (Aerial Symbol Outline)
- **考查点**：符号大纲侧边栏 (`aerial.nvim`)
- **目标文件**：`examples/playground/engine.py`
- **任务目标**：在大文件中快速定位函数，不靠肉眼翻滚。
- **操作步骤**：
  1. 打开 `examples/playground/engine.py`。
  2. 按快捷键 **`<leader>cs`**。
  3. 观察左侧：弹出了一个清爽的大纲树，列出了 `CalculationEngine` 及其下属的所有方法。
  4. 在大纲窗口内按 `j`/`k` 移动到 `execute_data_normalization`，按回车 `<CR>`。
  5. 再次按 **`<leader>cs`** 关闭大纲侧边栏。
- **通关验证**：主编辑区光标准确跳转到了该方法的代码行，左侧侧边栏平滑收起。

---

## 第二关：精准光标与极速移动篇

### 第 06 题：两键屏幕瞬移 (Flash Jump)
- **考查点**：无脑跳跃插件 (`flash.nvim`)
- **目标文件**：`examples/ui_playground.py`
- **任务目标**：从屏幕最上方瞬间跳到第 138 行的目标字符串上，不按一次 `j` 或方向键。
- **操作步骤**：
  1. 打开 `examples/ui_playground.py`，确保光标在第 1 行。
  2. 屏幕下方肉眼可见第 138 行有 `TARGET_ALPHA`。
  3. 在普通模式下按字母 **`s`**。
  4. 紧接着输入两个字母：**`ta`**。
  5. 此时屏幕上所有 `ta` 出现的地方都变成了一个醒目的单字母提示标签（例如 `h`、`k` 等）。
  6. 按下对应的提示字母。
- **通关验证**：光标以零延迟瞬移到了 `TARGET_ALPHA` 处！

---

### 第 07 题：搜索透镜雷达 (Hlslens Search Count)
- **考查点**：搜索计数透镜 (`nvim-hlslens`)
- **目标文件**：`examples/playground/engine.py`
- **任务目标**：搜索变量并观察每一次跳转的匹配计数。
- **操作步骤**：
  1. 打开 `examples/playground/engine.py`。
  2. 把光标放在第 24 行的 `audit_checksum_token` 上，按下按键 **`*`**（向后搜索当前词）。
  3. 观察每一个匹配词右侧自动浮现的半透明气泡透镜（例如 `[1/3]`）。
  4. 连续按 **`n`** 跳到下一个匹配，按 **`N`** 跳到上一个。
- **通关验证**：透镜气泡实时跟随光标刷新序号与相对行距！

---

### 第 08 题：语法节点结构跳跃 (Treesitter Textobjects Jump)
- **考查点**：基于 AST 的跨函数/跨类跳跃 (`]m` / `[m`)
- **目标文件**：`examples/playground/models.py`
- **任务目标**：只在方法之间精准跳转，跳过所有内部属性行。
- **操作步骤**：
  1. 打开 `examples/playground/models.py`，光标在文件顶部。
  2. 按 **`]m`**：光标瞬间跳到第一个方法（`is_settled`）的 `def` 行。
  3. 再按 **`]m`**：光标跳到下一个方法（`mark_completed`）。
  4. 再按 **`]m`**：光标跳到下一个方法（`mark_failed`）。
  5. 按 **`[m`**：光标倒退回上一个方法。
- **通关验证**：按键只在 `def` 语法节点间穿梭，丝毫不受中间普通代码干扰。

---

### 第 09 题：全局模糊检索飞索 (Snacks Picker)
- **考查点**：工程全局文件与文本搜索 (`<leader><space>` / `<leader>sg`)
- **任务目标**：不记路径，用片段关键字找到任何文件。
- **操作步骤**：
  1. 在 Neovim 中按下 **`<leader><space>`**。
  2. 在弹出的居中搜索框中输入 `engi`，回车。
  3. 瞬间打开了 `engine.py`！
  4. 再按下 **`<leader>sg`**（Search Grep），输入 `fresh_cyan`。
- **通关验证**：弹窗列出了所有包含该文本的文件行，按回车即可直接跳入对应行。

---

## 第三关：高能文本对象与并发编辑篇

### 第 10 题：函数参数一击必改 (Inner Argument Textobject)
- **考查点**：修改函数参数 (`mini.ai` 的 `cia` / `dia`)
- **目标文件**：`examples/playground/engine.py`
- **任务目标**：不用手动按 `w` 或退格，一键改写长参数。
- **操作步骤**：
  1. 打开 `engine.py`，定位到第 86 行的 `execute_data_normalization` 函数签名。
  2. 把光标随便停在参数 `param_lower_bound: float = 0.0` 的任意位置上。
  3. 在普通模式下直接键入：**`cia`**（Change Inner Argument）。
  4. 观察：整段参数定义被瞬间清空，且直接进入插入模式！
  5. 键入新参数 `min_val: float = -1.0`，按 `Esc`。
- **通关验证**：只改动了这一个逗号之间的参数，周围的参数与其他括号完好无损。

---

### 第 11 题：整个函数/类一键选中 (Function & Class Textobjects)
- **考查点**：语法块全量选择 (`vaf` / `vif` / `vac`)
- **目标文件**：`examples/playground/service.py`
- **任务目标**：快速选中并复制整个方法。
- **操作步骤**：
  1. 光标停在 `process_single_transaction` 函数体内任意一行。
  2. 键入：**`vaf`**（Visual Around Function）。
  3. 观察：整个函数的 `def` 声明、Docstring、全部内部逻辑被全部高亮选中！
  4. 键入 **`y`** 复制，移动到末尾按 **`p`** 粘贴。
- **通关验证**：无需用鼠标拖拉，一个动作完成全量函数复制。

---

### 第 12 题：全文无脑一键复制 (Global Buffer Textobject)
- **考查点**：全局文本对象 (`mini.ai` 的 `yag`)
- **目标文件**：任意文件
- **任务目标**：不再使用笨拙的 `ggVGy` 或全选命令。
- **操作步骤**：
  1. 在文件任意位置，普通模式下直接按下三个字母：**`yag`**（Yank Around Global）。
  2. 观察底部状态栏或消息提示已复制整篇文件行数。
- **通关验证**：全文已被复制进剪贴板。

---

### 第 13 题：多光标并发词汇编辑 (Multi-Cursor by Word)
- **考查点**：多光标并发修改 (`vim-visual-multi` 的 `Ctrl+n`)
- **目标文件**：`examples/playground/engine.py`
- **任务目标**：同时将 4 行重复的前缀一次性修改完毕。
- **操作步骤**：
  1. 打开 `engine.py`，找到第 72 行的 `initialize_metrics_counters` 函数。
  2. 看到连续 4 行都是 `metric_counter_*`。
  3. 光标停在第 73 行的 `metric_counter` 单词上，按 **`Ctrl+n`**。
  4. 此时第 1 个词被高亮选中；继续按 **`Ctrl+n`** 三次！
  5. 此时 4 行的 `metric_counter` 均出现了活动光标！
  6. 按键盘字母 **`c`**，输入新名字 `stat_kpi`，然后按两次 **`Esc`** 退出。
- **通关验证**：4 行前缀在 1 秒之内同时被完美修改为 `stat_kpi_*`！

---

### 第 14 题：垂直列多光标批处理 (Vertical Cursor Column)
- **考查点**：上下垂直添加光标 (`Ctrl+j` / `Ctrl+k`)
- **任务目标**：在连续几行行首同时插入注释符号 `# `。
- **操作步骤**：
  1. 在任意几行连续代码前，普通模式下按 **`Ctrl+j`** 连续 3 次向下添加垂直光标。
  2. 按大写 **`I`** 进入行首插入模式，输入 `# [TODO]: `。
  3. 按两次 `Esc` 退出。
- **通关验证**：3 行同时被插入了注释标记。

---

### 第 15 题：智能闭合括号与跳出 (Auto Pairs)
- **考查点**：自动配对与跳出 (`mini.pairs`)
- **任务目标**：打字时不手动输入闭合符号。
- **操作步骤**：
  1. 在任意空白行输入 `print(`。
  2. 观察：`mini.pairs` 瞬间自动补齐了右括号 `)` 并将光标放在两者中间。
  3. 继续输入 `"hello`，右双引号自动补齐。
- **通关验证**：无需手动移动光标去闭合符号。

---

## 第四关：折叠与视觉透视篇

### 第 16 题：行号折叠三角与鼠标点击交互 (Ufo Statuscolumn Fold Triangle)
- **考查点**：IDE 级行号折叠指示器与鼠标点击直接折叠 (`snacks.statuscolumn` + `nvim-ufo`)
- **目标文件**：`examples/playground/engine.py`
- **任务目标**：体验像 VSCode/IDE 一样在行号旁观察折叠三角，并直接用鼠标点击展开/折叠。
- **操作步骤**：
  1. 打开 `engine.py`，观察屏幕左侧行号区域：
     - 在可以折叠的行（如第 33 行 `def compute_statistical_metrics`）旁侧，出现了一个小三角指示器 **``**！
  2. 拿起鼠标，**直接移动鼠标指针点击左侧行号旁的 `` 三角图标**！
  3. 观察折叠后的视觉呈现：
     - 函数被瞬间折叠，三角箭头变为向右的折叠状态 **``**！
     - 这一行的右侧显示出精美的胶囊徽标：`⋯ 36 lines 󰁂`。
  4. 再次用鼠标点击向右的三角 **``**（或按下按键 `zo`）。
- **通关验证**：无需键盘命令，鼠标单次点击直接实现现代 IDE 级别的代码块折叠与展开！

---

### 第 17 题：折叠悬浮偷看神技 (Ufo Peek Folded Lines)
- **考查点**：无需展开即可窥探折叠内部 (`zp`)
- **目标文件**：`examples/playground/engine.py`
- **任务目标**：不破坏大纲折叠状态，直接查看折叠块内部代码。
- **操作步骤**：
  1. 重新在 `def compute_statistical_metrics` 行按 **`zc`** 将其折叠。
  2. 保持光标在折叠行上，按下按键：**`zp`**。
  3. 观察屏幕中央！
- **通关验证**：立即弹出了一个漂亮的悬浮预览窗口，完整展示了函数内部的 30 多行算法细节！无需按 `zo` 展开再折回。

---

### 第 18 题：全剧终极折叠视野 (Fold All / Unfold All)
- **考查点**：大纲级别全局折叠 (`zM` / `zR`)
- **目标文件**：`examples/playground/service.py`
- **任务目标**：一秒让复杂模块变成极简大纲。
- **操作步骤**：
  1. 打开 `service.py`，在普通模式下按下：**`zM`**（大写 M）。
  2. 观察：整个文件的全部 4 个方法同时收缩，整屏只剩下类名与方法签名！
  3. 按下按键：**`zR`**（大写 R）。
- **通关验证**：按 `zR` 瞬间全文所有折叠块同时展开，恢复完整编辑状态。

---

### 第 19 题：彩虹括号深度巡航 (Rainbow Delimiters)
- **考查点**：AST 括号逐级着色 (`rainbow-delimiters.nvim`)
- **目标文件**：`examples/playground/models.py`
- **任务目标**：肉眼一眼分清第 7 层嵌套。
- **操作步骤**：
  1. 打开 `models.py`，移动到第 32 行的 `PIPELINE_VALIDATION_SCHEMA` 字典。
  2. 观察从第 33 行开始逐层递进的括号：
     - 最外层 `{}`：黄色
     - `clusters` 列表 `[]`：青色
     - 元组 `()`：蓝色
     - `matrix` 多维数组：橙色、绿色、紫色
  3. 把光标放在深层的 `(40, 50)` 括号上。
- **通关验证**：光标所在的左右两个括号呈现完全一致的匹配颜色，层次清晰无误。

---

## 第五关：色彩魔法与界面调优篇

### 第 20 题：颜色背景透视 (Hex Color Highlighting)
- **考查点**：代码中十六进制色彩实时预览 (`mini.hipatterns`)
- **目标文件**：`examples/playground/config.py`
- **任务目标**：验证 HEX 颜色文本背景的真实渲染。
- **操作步骤**：
  1. 打开 `config.py`，定位到第 20 行的 `COLOR_PALETTE`。
  2. 观察每一行的文本，例如 `#ffff00`、`#00ffff`、`#ff5050`。
  3. 在空白处随手写一行：`TEST_COLOR = "#a855f7"`。
- **通关验证**：文本背景直接显示为鲜明的紫色背景，修改字符背景实时跟随变换！

---

### 第 21 题：图形化调色板与格式转换 (Color Picker & Converter - ccc.nvim)
- **考查点**：交互式颜色选取与格式转换 (`ccc.nvim`)
- **目标文件**：`examples/playground/config.py`
- **任务目标**：调出图形化颜色滑块调节颜色，并将 HEX 转换成 RGB。
- **操作步骤**：
  1. 在 `config.py` 中，光标停在 `#00cd00` 上。
  2. 按下快捷键 **`<leader>cp`**（或输入命令 `:CccPick`）并回车。
  3. 观察弹出的交互式调色窗口，按 `h`/`l` 调节滑块，按回车确定并替换。
  4. 再次把光标停在颜色上，按下快捷键 **`<leader>cC`**（或输入命令 `:CccConvert`）并回车。
- **通关验证**：十六进制 `#00cd00` 瞬间被转换成了 `rgb(0, 205, 0)`！同时常规代码行仍由 `mini.hipatterns` 提供零延迟的背景着色。

---

### 第 22 题：经典轨道滚动条与柔和分屏 (Satellite & Dimmed Splits)
- **考查点**：右侧灰色轨道滚动条与柔和分屏线
- **任务目标**：体验无干扰的传统 UI 视效。
- **操作步骤**：
  1. 打开任意长文件（如 `ui_playground.py`）。
  2. 使用 `Ctrl+d` / `Ctrl+u` 上下滚动翻页。
  3. 观察屏幕最右边缘：拥有一条连续的深灰背景轨道（`#25252a`），亮黄色滑块在轨道内滑动。
  4. 输入分屏命令：`:vsplit`。
- **通关验证**：中央的纵向分割线呈低调内敛的灰色（`#333338`），既清晰区分窗口又完全不刺眼抢戏。

---

## 第六关：剪贴板时空穿梭篇

### 第 23 题：原地时光倒流粘贴 (Yank History Cycle)
- **考查点**：原地轮换粘贴历史 (`yanky.nvim` 的 `[p` / `]p`)
- **目标文件**：`examples/playground/main.py`
- **任务目标**：连续复制 3 样东西，一次性找到刚才最早复制的那一份。
- **操作步骤**：
  1. 在 `main.py` 中，光标停在第 1 行用 `yy` 复制第一句注释。
  2. 移到第 35 行用 `yy` 复制 import 语句。
  3. 移到第 45 行用 `yy` 复制初始化语句。
  4. 移动到文件底部空白行，按下按键 **`p`** 粘贴（此时粘贴出来的是最后复制的那句）。
  5. ★ 关键操作：**立刻连续按下 `[p` 两次！**
- **通关验证**：刚才粘贴出来的文字在原地如走马灯一样，连续切换回了第 2 次复制、以及第 1 次复制的内容！按 `]p` 则向前轮换。

---

### 第 24 题：剪贴板历史弹窗打捞 (Yank Snacks Picker)
- **考查点**：剪贴板全局数据库检索 (`<leader>fy`)
- **任务目标**：从几小时前的复制历史中精确打捞一段文字。
- **操作步骤**：
  1. 在普通模式下按下快捷键：**`<leader>fy`**。
  2. 屏幕中央弹出了极速的 Snacks 浮窗，展示完整的剪贴板历史列表与高亮预览。
  3. 输入关键字过滤，按回车直接将选中的历史记录粘贴到光标处。
- **通关验证**：所有复制过的片段均持久化保存在 sqlite 中，通过统一的 Snacks Picker 一键打捞。

---

### 第 25 题：跨应用系统剪贴板互通 (Clipboard unnamedplus)
- **考查点**：系统级剪贴板集成
- **任务目标**：从外部网页/终端复制文字并贴入 Neovim，反之亦然。
- **操作步骤**：
  1. 在外部浏览器复制一段任意文本。
  2. 回到 Neovim，普通模式直接按 **`p`**，外部文字被直接粘贴进来。
  3. 在 Neovim 中选中文本按 **`y`**，切换到外部终端按 `Ctrl+Shift+v`，文字成功贴出。
- **通关验证**：无需配置额外的 `"+y` 或寄存器指定，全局无缝互通。

---

## 第七关：Git 审查与版本时间机篇

### 第 26 题：行尾静默 Blame 探针 (Current Line Blame Toggle)
- **考查点**：单行 Git 追溯与开关 (`gitsigns.nvim` 的 `<leader>ub`)
- **目标文件**：`examples/playground/service.py`
- **任务目标**：查看单行提交历史，并在需要专注时一键关闭它。
- **操作步骤**：
  1. 打开 `examples/playground/service.py`。
  2. 把光标停留在任意一行代码上静止 300 毫秒。
  3. 观察行尾：自动浮现浅灰色的提交信息：`tetsuya, <时间> • feat: ...`。
  4. 按下快捷键：**`<leader>ub`**。
  5. 再次移动光标：行尾 Blame 已经彻底隐藏，屏幕进入极简无扰模式！
  6. 再次按 **`<leader>ub`** 重新恢复显示。
- **通关验证**：随心开闭行尾 Blame 提示。

---

### 第 27 题：修改块跳跃与即时预览 (Hunk Navigation & Preview)
- **考查点**：Git Hunk 操作 (`]h` / `[h` / `<leader>gp`)
- **目标文件**：`examples/playground/main.py`
- **任务目标**：修改代码后就地审查 diff。
- **操作步骤**：
  1. 打开 `main.py`，把第 46 行的代码随手改几个字符。
  2. 观察屏幕最左侧的行号列：出现了一条鲜亮的竖线修改标记 `▎`。
  3. 将光标移到别处，按 **`[h`**：光标瞬间跳回刚才修改的这块代码！
  4. 按快捷键 **`<leader>gp`**（Git Preview Hunk）。
- **通关验证**：弹出一个精致的对比小浮窗，绿色显示改动后的行，红色显示原代码行！

---

### 第 28 题：专业双屏并排 Diffview (Diffview Full Workspace)
- **考查点**：全功能 Git 对比工作区 (`diffview.nvim`)
- **目标文件**：任意文件
- **任务目标**：像在 GitHub PR 页面一样并排审查全仓变更。
- **操作步骤**：
  1. 在普通模式下键入快捷键：**`<leader>gd`**（或 `<leader>gv`）。
  2. 观察工作区布局：
     - 左侧：树形列出本次所有修改过的文件列表。
     - 右侧：双屏左右并排显示修改前与修改后的高精度逐行对齐 Diff！
  3. 按键入 **`<leader>gD`**：可专门查看当前单个文件的版本演进历史。
  4. 审查完毕，按下快捷键：**`<leader>gq`**（Quit）。
- **通关验证**：Diffview 瞬间干净关闭，丝滑返回刚才正在编辑的文件位置。

---

### 第 29 题：内嵌终端 Lazygit (Terminal Lazygit)
- **考查点**：Snacks 呼出 Lazygit (`<leader>gg`)
- **任务目标**：不跳出 Neovim 提交代码。
- **操作步骤**：
  1. 按下快捷键：**`<leader>gg`**。
  2. Neovim 内部弹出了全屏的终端图形化 `lazygit` 界面。
  3. 查看分支、日志或按 `q` 退出。
- **通关验证**：平滑返回代码编辑，零上下文切换损失。

---

## 第八关：文件系统与工程级重构篇

### 第 30 题：像编辑文本一样管理目录 (Oil.nvim)
- **考查点**：文本化目录编辑器 (`oil.nvim` 的 `-` / `<leader>o`)
- **任务目标**：一秒重命名或新建文件，不需要命令行 `mv` / `touch`。
- **操作步骤**：
  1. 在 `main.py` 中，普通模式下直接按下键盘上的减号键：**`-`**。
  2. 观察：当前目录（`examples/playground/`）被作为一个普通的文本 buffer 打开，每个文件就是一行！
  3. 像平时改代码一样，把光标移到某一行，用普通编辑命令修改文件名。
  4. 输入 `:w` 保存！
- **通关验证**：磁盘上的真实文件名瞬间完成了重命名！再次按 `-` 可进入上级目录。

---

### 第 31 题：经典左侧工程树管理 (Neo-tree Explorer)
- **考查点**：工程侧边栏 (`neo-tree.nvim` 的 `<leader>e`)
- **任务目标**：侧边栏查看树状目录与快捷文件操作。
- **操作步骤**：
  1. 按下快捷键 **`<leader>e`**。
  2. 左侧展开工程文件树，在树上按 `j`/`k` 移动。
  3. 停在任意目录按 `a` 可新建文件，按 `d` 可删除文件，按 `?` 查看帮助菜单。
  4. 再次按 **`<leader>e`** 收起侧边栏。
- **通关验证**：左侧文件树收放自如。

---

### 第 32 题：项目级超强全局批量查找替换 (Grug-far Search & Replace)
- **考查点**：两栏全局正则替换 (`grug-far.nvim` 的 `<leader>sr`)
- **任务目标**：在整个项目中把所有的 `DAILY-SETTLE` 批量换成 `NIGHT-BATCH`。
- **操作步骤**：
  1. 按下快捷键 **`<leader>sr`**。
  2. 此时弹出一个双栏替换工作区：
     - 左上栏：输入要搜索的词 `DAILY-SETTLE`
     - 左下栏：输入要替换的目标词 `NIGHT-BATCH`
     - 右侧栏：实时列出全项目所有将受影响的文件与行号！
  3. 确认无误后，按下 `<leader>s` 或按页面提示按钮一键全部替换！
- **通关验证**：全项目所有相关文件瞬间批量替换完毕，干净彻底！

---

## 第九关：IDE 级状态栏交互与悬浮菜单篇

### 第 33 题：触觉按压反馈与文件路径菜单 (File Actions & Tactile Feedback)
- **考查点**：底部状态栏鼠标交互与触觉高亮反馈
- **任务目标**：体验 IDE 级鼠标按压变色反馈，通过浮动菜单快速执行文件操作，或双击一键复制完整路径。
- **操作步骤**：
  1. 用鼠标单击底部状态栏中央的**文件名或路径区域**（如 `main.py`）。
  2. **观察触觉反馈**：底栏该按钮区域瞬间触发高亮蓝色（`#0064c8`）按压反馈，180ms 后平滑恢复。
  3. **观察悬浮菜单**：正上方弹出一个精致的居中浮动菜单：
     - `[1] 󰅍 复制相对路径`
     - `[2] 󰅎 复制绝对路径`
     - `[3] 󰈤 复制文件名`
     - `[4] 󰉖 在 Oil 中打开当前目录`
     - `[5] 󰙅 在 Neo-tree 中展开定位`
     - `[6] 󰋚 查看 Diffview 文件历史`
     - `[7] 󰑕 重命名文件`
  4. 可以用鼠标直接点击某一项，或用键盘按 **`j` / `k`** 移动光标并按 **`<CR>`** 执行，按 **`<Esc>`** 或 **`q`** 随手关闭。
  5. 尝试快速**双击**底栏文件名区域：直接将文件完整绝对路径拷贝至系统剪贴板，右下角弹出绿色成功通知！
- **通关验证**：鼠标单击能弹出动作菜单，双击一秒复制路径。

---

### 第 34 题：文件编码与换行符即时切换菜单 (Encoding & Line Format Switcher)
- **考查点**：底栏编码与格式切换
- **任务目标**：像在 VSCode/JetBrains 中一样，一键转换文件编码（UTF-8、GB18030、Shift-JIS、EUC-JP）或换行符（LF / CRLF）。
- **操作步骤**：
  1. 用鼠标单击底部状态栏右侧的 **`UTF-8`**（或行尾换行符）区域。
  2. 观察：该区域闪烁触觉按压反馈，正上方弹出编码与格式切换菜单：
     - `[1] 󰉿 设为 UTF-8`
     - `[2] 󰉿 设为 GB18030 (简体中文/GBK兼容)`
     - `[3] 󰉿 设为 Shift-JIS (日文 Windows)`
     - `[4] 󰉿 设为 EUC-JP (日文 Unix)`
     - `[5] 󰑓 重新以指定编码加载 (edit ++enc=...)`
     - `[6] 󰌒 转换为 Unix 换行符 (LF)`
     - `[7] 󰌒 转换为 Windows 换行符 (CRLF)`
  3. 选中对应项回车，底栏的编码标签和换行符即刻动态刷新！
- **通关验证**：告别繁琐的 `:set fileencoding=...`，可视化随时切换。

---

### 第 35 题：Git 快捷控制台与 LSP 智能运维 (Git & LSP Context Menu)
- **考查点**：版本控制与语言服务快捷菜单
- **任务目标**：鼠标单击直接唤起深层开发运维工具。
- **操作步骤**：
  1. 用鼠标单击底栏左侧的 **Git 分支名称**（如 `main` 或带有修改数的徽标）：
     - 弹出 Git 快捷控制台：包含 Diffview 全局审查、Lazygit 终端、分支历史、一键开关行尾 Blame、修改块预览等。
  2. 用鼠标单击底栏右侧的 **LSP 伺服器名称**（如 `pyright`）：
     - 弹出语言服务控制面板：包含 LSP 状态信息、快速修复 (Code Action)、代码符号大纲、自动代码格式化、以及 LSP 服务器重启。
  3. 用鼠标单击最左侧的模式徽标（如 **`NORMAL`**）：
     - 呼出全局快速启动台：可快速启动 WhichKey 按键备忘、Telescope 搜索、Neo-tree 或 Lazy 插件管理器。
- **通关验证**：整个状态栏化身集成控制台，兼顾键盘极客与直观鼠标交互。

---

## 第十关：全工程内容搜索与批量替换篇

### 第 36 题：全工程实时文本雷达 (Live Grep)
- **考查点**：基于 Snacks Picker 的项目全局实时内容过滤与预览 (`<leader>/` 或 `<leader>sg` / `<leader>fg`)
- **任务目标**：无需打开具体文件，秒级搜索全工程代码内容，并在实时预览窗中准确定位目标。
- **操作步骤**：
  1. 在 Neovim 中处于普通模式，按下按键：**`<leader>/`**（或键入 **`<leader>sg`**）。
  2. 观察：屏幕中央瞬间弹出一个半透明搜索浮窗，左侧输入栏带有极速实时搜索光标，右侧为全尺寸代码高亮实时预览区。
  3. 在搜索栏键入：`PIPELINE_VALIDATION_SCHEMA`。
  4. 观察：搜索列表精准过滤出 `models.py` 中的定义位置与引用位置，右侧实时显示多层嵌套彩虹字典结构。
  5. 按键盘 **`Ctrl+j` / `Ctrl+k`**（或上下方向键）在不同搜索结果之间切换，右侧预览窗同步跟随滚动。
  6. 按 **`<CR>`**（回车键）：浮窗关闭，光标瞬间跳入 `models.py` 的对应行！
- **通关验证**：成功使用 `<leader>/` 搜索并跳转，体验零延迟的 live grep 索引与代码上下文联动。

---

### 第 37 题：光标符号引力捕获 (Grep Word under Cursor)
- **考查点**：一键提取光标所在符号并全局交叉检索 (`<leader>fw` 或 `<leader>sw` / `<leader>fc`)
- **目标文件**：`examples/playground/service.py`
- **任务目标**：对于长变量名或业务实体，免手动输入拼写，一键查询其在全项目中的全部出现位置。
- **操作步骤**：
  1. 打开调度文件：`nvim examples/playground/service.py`
  2. 移动光标至第 15 行的导入类名 **`BatchTransactionRecord`** 上。
  3. 无需进入选择模式，直接按下：**`<leader>fw`**（或按 **`<leader>sw`**）。
  4. 观察：Snacks 自动将当前光标词捕获为检索关键字，列出：
     - `models.py`：类定义 `@dataclass class BatchTransactionRecord:`
     - `service.py`：模块导入与实例化调用
     - `main.py`：处理流水线遍历输出
  5. 在搜索列表中按回车直接跳转到目标调用点。
- **通关验证**：比手动打字快 5 倍，光标所在词全工程引用秒现。

---

### 第 38 题：双栏交互式全工程批量替换 (Grug-far Search & Replace)
- **考查点**：双栏式正则批量搜索替换 (`<leader>sr` / `<leader>sR`)
- **任务目标**：使用工业级重构面板，将全工程中某个旧的服务 ID 一键批量重构为新的标识符，且能在执行前逐行预览 Diff。
- **操作步骤**：
  1. 在任意文件中，按下按键：**`<leader>sr`**。
  2. 屏幕右侧平滑展开两栏式 **Grug-far** 交互式工作区：
     - 在 **`Search:`** 栏输入要搜索的旧文本：`SVC-ORCHESTRATOR-01`
     - 在 **`Replace:`** 栏输入要替换的新文本：`SVC-NIGHT-LAB-99`
  3. 观察下方结果区域：实时列出 `service.py` 中匹配到的行，并以红绿 Diff 样式精确展示替换前后的文本对比！
  4. 确认替换安全无误后，在面板中按 **`<leader>s`**（或按快捷替换键）执行替换，所有受影响的文件同时自动写盘保存。
  5. 按 **`<Esc>`** 或在控制行按 **`q`** 退出 Grug-far 面板。
  6. 进阶提示：如果只想替换当前正在编辑的单个文件，按下 **`<leader>sR`** 即可开启单文件作用域替换。
- **通关验证**：掌握带实时可视化 Diff 的安全重构利器，彻底摆脱传统命令行盲目正则替换的风险。

---

### 第 39 题：原生 Quickfix 管道批量跃迁 (Native `:grep` & Quickfix List)
- **考查点**：Neovim 底层 `grepprg`（ripgrep）与 Quickfix 列表联动
- **任务目标**：掌握 Unix 管道哲学下的原生批处理检索流，适用于无 UI 插件或深度脚本自动化场景。
- **操作步骤**：
  1. 在底行命令行键入：`:grep CalculationEngine` 并回车。
  2. 观察底部提示：`rg --vimgrep` 瞬时检索完毕，结果已注入全局 Quickfix 列表。
  3. 键入命令：**`:copen`** 打开底部 Quickfix 面板，查看结构化的搜索条目列表。
  4. 键入 **`:cnext`**（或简写 `:cn`）：光标直接跳入下一个搜索匹配点！
  5. 键入 **`:cprev`**（或简写 `:cp`）：光标返回上一个匹配点！
  6. 处理完毕后，键入 **`:cclose`**（或在列表中按 `q`）关闭 Quickfix 窗口。
- **通关验证**：理解原生 Quickfix 机制，掌握无头环境与标准流下的全局检索控制力。

---

## 🏆 通关成就清单 (Checklist)

完成练习后，可在每一项后面打勾标记：

- [ ] 关卡一：跨文件跳转 `gd` 与文档 `K` 运用自如
- [ ] 关卡二：Flash 两字瞬移 `s` 彻底取代狂按 `j/k`
- [ ] 关卡三：参数修改 `cia` 与多光标 `Ctrl+n` 肌肉记忆建立
- [ ] 关卡四：代码折叠 `zc` 与悬浮偷看 `zp` 熟练使用
- [ ] 关卡五：调色板 `:CccPick` 与颜色直观预览
- [ ] 关卡六：循环粘贴 `[p` / `]p` 成为复制利器
- [ ] 关卡七：Diffview 双屏对比 `<leader>gd` 成为日常代码审查习惯
- [ ] 关卡八：用 Oil `-` 直接编辑目录成为本能
- [ ] 关卡九：状态栏鼠标按压反馈、编码切换与浮动菜单运用自如
- [ ] 关卡十：实时全文搜索 `<leader>/`、光标词检索 `<leader>fw` 与批量替换 `<leader>sr` 运用自如

离线环境随时温故知新，祝您 Neovim 技艺更上一层楼！
