# 03 — 从文本记录到控制总额

`UNSTRING` 把逗号分隔的输入拆进字段；`IF` 判断状态，数值字段进入总额，
最后用 `STRING` 组装输出。控制总额的意义是让操作人员可以用记录数、成功
数、错误数和金额总计判断一次处理是否完整。

本练习中的 `000104,0000,OK` 会因金额下限检查变成 `NG`，而原本状态为
`NG` 的记录也会增加错误计数。程序仍然输出每条记录，方便安全重跑时做
逐条比对。

练习：新增一个 `WS-VALID-ACCOUNT` 条件，让账号不是 6 位数字时也失败，
并把它反映到 summary。先写测试数据，再写逻辑。

