# Neovim 界面与交互增强功能上手指南 (UI & Plugins Tutorial)

本指南配合 [`examples/ui_playground.py`](file:///home/tetsuya/development/night-batch-lab/examples/ui_playground.py) 使用，带您快速掌握近期配置的各项 Neovim 界面与交互增强插件。

---

## 🚀 快速开始

在终端中进入本仓库并打开演练文件：

```bash
cd /home/tetsuya/development/night-batch-lab
nvim examples/ui_playground.py
```

---

## 1. 颜色代码直观预览 (`mini.hipatterns`)

- **功能**：自动识别代码中的 HEX 颜色字符串（如 `#ffff00`、`#00ffff`、`#ff5050` 等），直接将其**背景着色**为该颜色的真实色彩。
- **演练位置**：查看 `ui_playground.py` 第 23~33 行的 `THEME_PALETTE` 字典。
- **操作体验**：
  1. 移动光标到颜色定义区域，无需光标悬停或外部插件命令，所有十六进制颜色值已直接以真实色彩呈现。
  2. 尝试在代码中随手敲一个颜色，例如 `#ff00ff` 或 `#123456`，它会立即实时渲染背景！

---

## 2. 彩虹嵌套括号高亮 (`rainbow-delimiters.nvim`)

- **功能**：基于 Treesitter AST 语法树，自动按嵌套深度为成对的 `()`、`[]`、`{}` 赋予不同的高对比度主题色彩。
- **演练位置**：查看 `ui_playground.py` 第 41~74 行的 `demo_nested_rainbow_structures()` 函数。
- **操作体验**：
  1. 观察 `config_tree` 内部从最外层的 `{` 到深层的 `(`、`[`、`{`。
  2. 每一层配对括号颜色严格对应：
     - 第 1 层：**亮黄色**
     - 第 2 层：**青色**
     - 第 3 层：**蓝色**
     - 第 4 层：**橙色**
     - 第 5 层：**绿色**
     - 第 6 层：**紫色**
     - 第 7 层：**红色**
  3. 当光标停在某个括号上时，对应闭合括号的层级色彩一目了然，再也不用数括号了。

---

## 3. 现代化语法高亮代码折叠 (`nvim-ufo`)

- **功能**：比原生折叠更智能、更快、更美观。折叠时**保留原行语法高亮**，并在尾部展示优雅的胶囊统计徽标 `⋯ N lines 󰁂`；支持不展开代码直接**悬浮窥视**折叠内容。
- **演练位置**：查看 `ui_playground.py` 第 87~146 行的 `NightBatchProcessor` 类。
- **快捷键演练**：
  | 快捷键 | 动作说明 | 推荐尝试场景 |
  |---|---|---|
  | `zc` | 折叠当前光标所在代码块 | 光标移动到 `def prepare_environment` 行按 `zc`，查看右侧胶囊徽标 |
  | `zp` | **折叠悬浮预览 (Peek)** | 在已折叠的代码行上按 `zp`，会弹出一个悬浮小窗口直接看里面写了什么！ |
  | `zo` | 展开当前折叠 | 在折叠行按 `zo` 展开 |
  | `zM` | **一键折叠全文** | 按 `zM`，整个文件的所有函数和类全部收缩，只留下大纲，视野极其清爽 |
  | `zR` | **一键展开全文** | 按 `zR`，全文所有折叠彻底展开恢复原样 |
  | `zm` / `zr` | 逐级折叠 / 逐级展开 | 适合多层循环、嵌套函数递增查看 |

---

## 4. 极简浅灰行尾 Git Blame (`gitsigns.nvim`)

- **功能**：
  - 光标停留在代码任意行 300ms，行尾自动浮现浅灰暗淡的提交作者、相对时间和 commit message，安静不打扰。
  - 左侧 Sign 列显示清晰的修改条标记（`▎` 新增/修改，`` 删除）。
- **快捷键演练**：
  | 快捷键 | 动作说明 |
  |---|---|
  | `<leader>ub` | **一键开/关行尾 Blame 显示**（属于 Snacks UI 开关体系） |
  | `]h` / `[h` | 跳到下一个 / 上一个 Git 修改块 (Hunk) |
  | `<leader>gp` | 悬浮预览当前修改块的 Diff 变动详情 |
  | `<leader>gs` / `<leader>gr` | 暂存 (Stage) / 撤销恢复 (Reset) 当前光标所在的修改块 |

---

## 5. 专业级 Git Diff 工作区 (`diffview.nvim`)

- **功能**：
  - 拥有左侧文件变更列表（Tree/List）以及右侧左右并排的双屏实时 Diff。
  - 支持查看当前工作区变更、历史提交、单文件版本变动轨迹。
- **快捷键演练**：
  | 快捷键 | 动作说明 |
  |---|---|
  | `<leader>gd` 或 `<leader>gv` | **打开 Diffview 工作区**（左侧看变动文件，右侧左右对比） |
  | `<leader>gD` | **打开当前文件的历史演进 (File History)**，可逐个 commit 查看历史变化 |
  | `<leader>gV` | **打开整仓分支提交历史 (Branch History)** |
  | `<leader>gq` | **一键退出并关闭 Diffview**，回到刚才编辑的代码界面 |
- **工作区内部操作**：
  - 在左侧文件面板按 `j`/`k` 移动，按 `<CR>` 打开该文件 diff。
  - 按 `tab` / `s-tab` 切换上一个/下一个文件。
  - 按 `s` 快速暂存（Stage）或取消暂存（Unstage）。

---

## 6. 滚动条与窗口视觉微调

- **传统 UI 滚动条 (`satellite.nvim`)**：
  - 在 `ui_playground.py` 中使用 `Ctrl+d` / `Ctrl+u` 或 `G` / `gg` 快速滚动。
  - 观察屏幕右边缘：拥有连续的深灰色背景轨道（`#25252a`），滚动条滑块为醒目的亮黄色（`#ffff00`），避免了透明覆盖背景时的突兀感。
- **低调内敛的分割线**：
  - 输入 `:vsplit` 打开左右分屏。
  - 观察中央的垂直分割线：颜色已调低为柔和暗淡的灰色（`#333338`），不再抢走代码主体的视线。
- **语义化状态栏 (`heirline.nvim`)**：
  - 底部状态栏现在按模式自动变色（NORMAL 亮黄、INSERT 绿色、VISUAL 紫色、COMMAND 蓝色）。
  - 文件路径中目录部分低调弱化、文件名高亮加粗，并伴有彩色文件图标与 Git 变更增减统计（`+N` / `~N` / `-N`）。

---

祝您在全新的 Neovim 界面与高效交互体系中编码愉快！随时可在 `examples/ui_playground.py` 中自由修改体验。
